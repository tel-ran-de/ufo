apiVersion: apps/v1
kind: Deployment
metadata:
  name: ufo-deployment
  namespace: ufo
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: ufo
  template:
    metadata:
      labels:
        app: ufo
    spec:
      containers:
        - name: ufo-service
          image: ghcr.io/tel-ran-de/ufo:64fe7afc67cd38ce0634f14555af8156706024eb
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      imagePullSecrets:
        - name: ghcr-secret
